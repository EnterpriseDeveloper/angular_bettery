export interface Answer {
  payToken:number;
  mintedToken: number;
  event_id: number;
  answer: number;
  from: string;
  answered: boolean;
  amount: number | string;
  betAmount: number;
  mintedToken: number;
  payToken: number;
}
